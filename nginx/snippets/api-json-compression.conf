# Include inside the host Nginx API proxy location when it bypasses Compose Nginx.
# Keep streaming responses (text/event-stream) outside the compression types.
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_comp_level 5;
gzip_types application/json;
